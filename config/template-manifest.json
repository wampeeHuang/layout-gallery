{
  "_notice": "唯一真相源 — 模板文件合约。validate-templates.js / add-template.js / server.js 都从此读取，禁止各自硬编码。",
  "_version": 1,

  "template_types": {
    "gallery": {
      "description": "Gallery 卡片模板 — 仅需 template.html",
      "required": ["template.html"],
      "optional": ["tokens.json", "template.json"]
    },
    "brand_kit": {
      "description": "品牌套件模板 — 需 template.html + tokens.json，双端可用",
      "required": ["template.html", "tokens.json"],
      "optional": ["template.json"]
    }
  },

  "default_type": "gallery",

  "validators": {
    "tokens_schema": {
      "module": "validate-templates",
      "function": "validateTokensSchema",
      "description": "校验 tokens.json 六分类结构 + brandKit 字段完整性"
    },
    "root_sync": {
      "module": "validate-templates",
      "function": "validateRootSync",
      "description": "校验 template.html :root 与 tokens.json 值一致"
    },
    "standard_vars": {
      "module": "validate-templates",
      "function": "validateStandardVars",
      "description": "强制标准 CSS 变量名 — 模板必须使用 token-contract.json 定义的标准词汇，方言零容忍"
    },
    "google_fonts": {
      "module": "validate-templates",
      "function": "validateGoogleFonts",
      "description": "Google Fonts 零容忍扫描 — template.html 和 tokens.json 双端检查"
    },
    "hardcoded": {
      "module": "validate-templates",
      "function": "validateHardcoded",
      "description": "硬编码审计 — 检测颜色/字体/圆角/动效是否应改为 var(--*) 引用"
    }
  }
}
